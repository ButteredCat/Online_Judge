#include<stdio.h>
#include<string.h>

typedef struct STACK
{
	char s[100];
	int top=-1;
} Stack;


int main()
{
	char s[100];
	Stack optr, opnd;
	while(scanf("%s", s) != EOF)
	{
		length = strlen(s);
		for(i=0; i<length; ++i)
		{
			switch(s[i])
			case:
		}
	}
	return 0;
}
